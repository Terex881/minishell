#include "../minishell.h"

t_var	*ft_new(void *value)
{
	// t_var	*node;
	(void)value;

	// node = malloc(sizeof(t_var));
	// // node = NULL;
	// if (!node)
		return (NULL);
	// *node->arg = ft_strdup(value);
	// node->next = NULL;
	// return (node);
}